<nav class="navbar navbar-expand-md bg-light border sticky-top custom-navbar">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand" href="/">
      <i class="fa-regular fa-compass" style="color: #ff385c;"></i>
    </a>

    <!-- Toggler button for mobile -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Center Nav Link -->
    <div class="navbar-nav d-none d-md-flex">
      <a class="nav-link" aria-current="page" href="/listings">Explore</a>
    </div>

    <!-- Search bar -->
    <div class="navbar-nav ms-auto search-bar d-none d-md-flex">
      <form class="d-flex" role="search">
        <input
          class="form-control me-2"
          type="search"
          placeholder="Search"
          aria-label="Search"
        />
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>

    <!-- Collapsible nav items -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings">Explore</a>
        <div class="nav-item w-100 d-block d-md-none p-2">
          <form class="d-flex" role="search">
            <input
              class="form-control me-2"
              type="search"
              placeholder="Search"
              aria-label="Search"
            />
            <button class="btn btn-outline-success" type="submit">Search</button>
          </form>
        </div>
        <a class="btn" href="/listings/new">Become a host</a>
        <% if (!currUser) { %>
        <a class="nav-link" href="/signup"><b>Sign Up</b></a>
        <a class="nav-link" href="/login"><b>Log In</b></a>
        <% } else { %>
        <a class="nav-link" href="/logout">Log Out</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<style>
  .custom-navbar {
    background-color: #f8f9fa !important; /* Ensures solid background */
  }

  .navbar-collapse {
    background-color: #f8f9fa; /* Mobile expanded menu bg */
    padding: 0.5rem 1rem;
  }

  @media (max-width: 768px) {
    .navbar-nav .nav-link,
    .navbar-nav .btn {
      padding: 0.5rem 1rem;
      font-size: 1rem;
    }

    .navbar-toggler {
      border: none;
    }

    .navbar-nav .nav-item form {
      width: 100%;
    }
  }

  @media (max-width: 576px) {
    .navbar-brand {
      font-size: 1.1rem;
    }
    .search-bar {
      display: none !important;
    }
  }
</style>
